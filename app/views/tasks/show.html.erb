<h1><%=@task.title %></h1>

<%= link_to 'update a task', edit_path %>
<%= link_to'back to tasks', tasks_path %>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
